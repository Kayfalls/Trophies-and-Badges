rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      // Allow public read
      allow read: if true;
      
      // Only admins can upload/modify product files
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}
